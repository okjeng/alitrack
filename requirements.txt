# AliTrack 백엔드 의존성
# pip install -r requirements.txt

# 웹 프레임워크
fastapi==0.111.0
uvicorn[standard]==0.29.0

# 설정 관리 (.env 로드)
pydantic-settings==2.2.1

# JWT 인증
python-jose[cryptography]==3.3.0

# HTTP 클라이언트 (알리 API 호출)
httpx==0.27.0

# Redis (Rate Limit + 캐시)
redis[asyncio]==5.0.4

# DB (PostgreSQL)
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.29

# Web Push (VAPID)
pywebpush==2.0.0
